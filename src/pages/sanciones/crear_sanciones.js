import React from 'react';
import {
    Grid,
} from '@mui/material';

const Crear_sanciones = () => {
    return (
        <form>
            <Grid container spacing={2}>
                
            </Grid>
        </form>
    );
};

export default Crear_sanciones;
