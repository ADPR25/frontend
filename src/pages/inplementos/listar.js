import React from 'react';
import {
    Grid,
} from '@mui/material';

const Inventario = () => {
    return (
        <form>
            <Grid container spacing={2}>
            </Grid>
        </form>
    );
};

export default Inventario;
